# Port your backend will run on
PORT=10000

# MongoDB connection string (replace NEW_PASSWORD with your actual password)
MONGO_URI=mongodb+srv://mall_user:NEW_PASSWORD@cluster0.cpfcfzr.mongodb.net/mall?retryWrites=true&w=majority

# Secret key for signing JWT tokens (pick any strong string)
JWT_SECRET=someSuperSecret123

git rm --cached .env
